# Add my 'web' type.
--type-add=web:*.{html,css,js,jsx,tsx}
--type-add=media:*.{gif,png,jpg}

# Disable for vim :Files
# --color=always

--hidden

# linked files
--follow

# Searches case insensitively if the pattern is all lowercase. Search case
# sensitively otherwise.
--smart-case

# Ignore
--glob=!.git/*
--glob=!*.pdf
--type-not=media

# all ignore-related filtering with the --no-ignore flag
# --no-ignore
